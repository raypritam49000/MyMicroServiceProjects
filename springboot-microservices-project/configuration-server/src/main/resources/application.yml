server:
  port: 8888

spring:
  application:
    name: configuration-server

  cloud:
    config:
      server:
        git:
          clone-on-start: true
          uri: https://github.com/SaiUpadhyayula/microservices-config-server

